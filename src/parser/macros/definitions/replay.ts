import type {AddMacroArgs} from "../types.js";

const replay: AddMacroArgs = [
    "replay",
    [
        {type: "optional"},
    ],
    (node, song, walker) => {
        // ignore
    },
]
export default replay;