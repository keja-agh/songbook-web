import type {AddMacroArgs} from "../types.js";

const memorize: AddMacroArgs = [
    "memorize",
    [
        {type: "optional"},
    ],
    (node, song, walker) => {
        // ignore
    },
]
export default memorize;